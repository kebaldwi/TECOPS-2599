# ============================================================================
# Authors
# ============================================================================
# | Name            | Role                              | Contact              |
# |-----------------|-----------------------------------|----------------------|
# | Igor Manassypov | Systems Engineer                  | imanassy@cisco.com   |
# Copyright © 2024-2026 Cisco Systems, Inc. All rights reserved.
# ============================================================================

---
- name: Discover Devices in Catalyst Center from devices.json
  hosts: catalyst_center
  gather_facts: no
  vars_files:
    - vault.yml
  vars:
    DEBUG: "{{ lookup('env', 'DEBUG') | default(false) | bool }}"

  module_defaults:
    cisco.dnac.discovery_workflow_manager:
      dnac_host: "{{ dnac_host }}"
      dnac_username: "{{ dnac_username }}"
      dnac_password: "{{ dnac_password }}"
      dnac_verify: "{{ dnac_verify }}"
      dnac_port: "{{ dnac_port }}"
      dnac_version: "{{ dnac_version }}"
      dnac_debug: "{{ dnac_debug }}"
      dnac_log_level: "{{ dnac_log_level }}"
      dnac_log: "{{ dnac_log }}"

  tasks:

    # ── Step 1: Load input data ───────────────────────────────────────────────
    - name: Resolve devices_json_path to absolute
      set_fact:
        _resolved_json_path: >-
          {{ devices_json_path if devices_json_path.startswith('/')
             else (playbook_dir + '/' + devices_json_path) }}

    - name: Load devices input JSON
      ansible.builtin.slurp:
        src: "{{ _resolved_json_path }}"
      register: _json_raw

    - name: Parse devices input JSON
      set_fact:
        devices_data: "{{ _json_raw.content | b64decode | from_json }}"

    - name: Validate that project key exists in input data
      assert:
        that: devices_data.project is defined and devices_data.project | length > 0
        fail_msg: "Input JSON must contain a non-empty 'project' list."
        success_msg: "Input data loaded — {{ devices_data.project | length }} entries found."

    # ── Step 2: Build discovery config list ──────────────────────────────────
    #
    # Each entry in devices.json that has a non-null DeviceList produces one
    # discovery task.  The DeviceList is a comma-separated string of IPs that
    # is split into the module's ip_address_list.
    #
    # Discovery uses the global credentials already configured in Catalyst
    # Center (cli_credentials_list references description + username only).
    # discovery_type MULTI RANGE is used so that non-contiguous individual
    # IPs are each treated as a separate target.
    #
    - name: Build discovery config list
      set_fact:
        discovery_list: >-
          {%- set ns = namespace(result=[]) -%}
          {%- for entry in devices_data.project -%}
            {%- if entry.DeviceList -%}
              {%- set ips = entry.DeviceList.split(',') | map('trim') | list -%}
              {%- set disc = {
                'discovery_name': entry.Project | default('Device-Discovery'),
                'discovery_type': 'MULTI RANGE',
                'ip_address_list': ips,
                'protocol_order': 'ssh',
                'retry': 5,
                'timeout': 3,
                'net_conf_port': '830',
                'preferred_mgmt_ip_method': 'UseLoopBack',
                'global_credentials': {
                  'cli_credentials_list': [{'description': 'CLI-net-admin', 'username': 'net-admin'}],
                  'snmp_v2_read_credential_list': [{'description': 'RO'}],
                  'snmp_v2_write_credential_list': [{'description': 'RW'}],
                  'net_conf_port_list': [{'description': 'NETCONF-net-admin'}]
                }
              } -%}
              {%- set ns.result = ns.result + [disc] -%}
            {%- endif -%}
          {%- endfor -%}
          {{ ns.result }}

    - name: --DEBUG-- Discovery config list
      when: DEBUG
      debug:
        msg: "{{ discovery_list }}"

    - name: Validate discovery list is non-empty
      assert:
        that: discovery_list | length > 0
        fail_msg: "No entries with DeviceList found in input JSON."
        success_msg: "{{ discovery_list | length }} discovery task(s) to run."

    # ── Step 3: Run device discovery ─────────────────────────────────────────
    - name: Discover devices — "{{ item.discovery_name }}"
      cisco.dnac.discovery_workflow_manager:
        state: merged
        config:
          - "{{ item }}"
      loop: "{{ discovery_list }}"
      register: discovery_results

    - name: --DEBUG-- Discovery results
      when: DEBUG
      debug:
        msg: "{{ discovery_results }}"

    # ── Step 4: Summary ────────────────────────────────────────────────────────
    - name: Device discovery complete
      debug:
        msg:
          - "Device discovery submitted successfully"
          - "Discovery tasks run: {{ discovery_list | length }}"
          - "Devices targeted: {{ discovery_list | map(attribute='ip_address_list') | flatten | join(', ') }}"
